# Classification Inference Samples

Default sample image path used by test and script:
- `images/sample.jpg`

Configuration (in `.env`):
- `CLASSIFICATION_TEST_IMAGE_FILE`: path to one test image file
- `CLASSIFICATION_EXPECTED_LABEL`: expected label for that image
